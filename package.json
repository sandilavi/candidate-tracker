{
  "name": "candidate-tracker",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web",
    "build:shared": "npm run build --workspace=packages/shared",
    "test": "npm run test --workspaces --if-present",
    "dev": "npx concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "build": "npm run build:shared",
    "db:migrate": "npm run db:migrate --workspace=@candidate-tracker/api",
    "db:seed": "npm run db:seed --workspace=@candidate-tracker/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
