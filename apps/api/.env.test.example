# Test Database connection string
# This points to the local docker-compose Postgres instance, but uses a separate schema
DATABASE_URL="postgresql://postgres:password@localhost:5432/candidate_tracker?schema=test"
